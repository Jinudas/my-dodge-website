import React from 'react';
import './HeroSection.css';

const HeroSection = () => {
  return (
    <div className="hero-container">
      <div className="hero-content">
        <h1 className="hero-heading">DODGE</h1>
        <p className="hero-paragraph">
         Unleash the beast within every ride, feel the engine roar, experience Dodge’s unmatched power, speed, and adrenaline with every mile.
        </p>
      </div>
    </div>
  );
};

export default HeroSection;
